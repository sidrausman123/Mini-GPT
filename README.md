# Mini-GPT
Mini GPT is a simplified implementation of a GPT-style Transformer model built from scratch using PyTorch. This project is designed for learning, experimentation, and understanding how large language models (LLMs) work internally, without the heavy compute requirements of full-scale GPT models.
